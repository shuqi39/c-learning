8.14：namespace叫做名称空间或者命名空间，用于组织代码，避免命名冲突

Main方法是程序入口，注意大小写

Console.WriteLine输出后换行，Console.WriteLine输出后不换行

8.15：隐式转换：由语言保证的类型安全的转换，不需要显式语法

数值类型的扩展转换

从派生类转换为基类

由语言或库定义的安全结构转换  
显式转换：通常无法由编译器保证转换安全，需要强制类型转换语法

可能丢失信息的数值类型转换

可能在运行时失败的引用类型转换  

Unity中的模型是由线框和渲染材质组成的，没有渲染材质就只是一堆三角框

8.16：成员（实例）字段、方法等 是属于某一个具体的对象，比如每个人都有自己 的年龄

静态字段、方法是属于类整体的，比如人类的总数不属于某一个具体的人

类成员是隐式private的，仅在类内可访问，如果希望任意位置可访问，需要声明为public  
方法，简而言之就是把做一件事情。做这件事情可能需要一些前提条件，
做好以后可能会给出反馈

比如，方法MakeBread用于制作面包，方法需要的原料是面粉、奶、糖，
制作完毕后它会给我们返回一个面包作为结果

Bread MakeBread(Flour f, Milk m, Sugar s)
{
// ... 这里是制作面包的过程
return deliciousBread;
}

8.17：int? a = null; 加上?可以让int类型的变量也能赋null  
“堆栈”实际上是指栈  
C# 两大主要类别：

值类型（Value types）

引用类型（Reference types）

值类型直接存储数据（实例）本身
值类型在以下场景都会发生值的复制：

赋值

向方法传递参数

从方法返回结果
对于值类型，复制指的是复制类型实例的本身  
C# 提供了以下内置值类型，也称为简单类型：

整型数值类型

浮点数值类型

bool - 布尔类型

char - Unicode UTF-16 字符
结构体 struct 是值类型（简单类型本质上就是struct类型），但是以上简单类型、额外支持一些额外特性：

可以使用字面量表示值，例如

可以使用 const 关键字声明简单类型的常量

如果一个表达式的操作数全部是简单类型常量，则该常量表达式会在编译期计算  
引用类型变量存储的是数据（实例）的引用，两个变量可以同时引用同一个对象；因此，
对其中一个变量所做的操作，可能会影响到另一个变量所引用的对象
关键字 class interface delegate record 用于声明引用类型
C#还提供了 dynamic object string 作为内置引用类型  
堆就是一大块内存，创建引用类型的实例的时候会自动从堆上分配，
我们写代码的时候无需关心具体分配到哪了，也不需我们负责要释
放，因为这都是运行时帮我们做的事情。
需要知道的唯一一件事情，就是我们拥有了该实例的引用  
栈

是后进先出(Last In First Out) 的一种数据结构

程序运行时会维护调用栈，每个方法调用都有自己的栈帧，方法结束后，栈帧被移除

局部变量就存在于方法的栈帧中，参数也可以通过栈帧传递  
unity中awake:一运行就会运行  
onenable启动组件时会运行  
start在启动组件时onenable之后运行  
update在启动组件后每帧运行  
lateupdate在启动组件后跟随update后每帧运行  
fixedupdate在启动组件后固定帧运行  
ondisable在未启动组件时运行

8.18：数组是一种用于存储同类型数据的集合(引用类型)，在内存中连续存放，通过索引（从 0 开始）访问元素。数组长度在创建后不可改变，适合存放数量固定的数据。

异常就是程序在运行中发生的错误或者预期之外的情况，
比如参数错误、空引用、文件不存在等

在C#中可以使用 try / catch / finally 处理异常  
将可能引发异常的代码放入 try 块中，在 catch 子句中指定要处理的异常类型

一个 try 可以对应多个 catch，但 catch 必须先尝试捕获具体的异常，后捕获宽泛的异常

抛出异常时按照从上到下的顺序进行匹配，每次抛出异常最多执行一个 catch 块

如果 catch 子句没有指定任何异常类型，则可以匹配所有类型异常

throw 可以显式抛出一个异常，比如 throw new OverflowException();

8.22：射线与线段不同的是，射线的第2个参数是将第1个参数给出的点，无论是多少看作0，0，0而确定的方向，而不是像线段一样，是线段的终点

8.23：ref：通过引用传参

in：通过只读引用传参

out：输出参数

params：参数数组

默认参数/可选参数  
重载指的是方法名称相同，但方法签名不同的方法

方法签名包括：参数类型、参数个数、参数顺序

凡是不能让编译器在调用点确定唯一目标方法的，都不会作为重载判断因素
8.24：1. 属性
属性是对字段的封装访问方式，可以保护数据安全，增加赋值和取值时的额外逻辑。属性可以是成员的，也可以是静态的

属性由称作访问器的方法组成

get: 没有参数，返回类型为属性声明的类型

set: 没有返回值，有一个名为value的隐式形参，类型和属性相同

init: 相当于特殊的set访问器，只能在对象初始化阶段使用，一旦对象创建完成，属性不可再被修改

上述访问器至少存在其一

属性通过上述访问器访问后备字段，如果使用自动属性也可以不存在后备字段，但自动属性的方法体必须省略

属性的本质是方法

2. 继承简介
类的继承是面向对象三大特性之一（封装、继承、多态），被继承的类叫：基类 或 父类，继承后的类叫：派生类 或 子类

继承允许一个类从另一个类获取已有的成员，因此有如下好处：

代码复用

结构清晰

方便扩展

继承的缺点：强耦合

语法：class 子类 : 父类

3. 析构方法
析构方法没有返回值和参数，名称为 ~ClassName

只有当类实例包含非托管资源，并且在实例销毁前需要做清理工作时，才考虑使用析构方法

4. 构造方法
构造方法名必须和类名完全相同，并且没有返回值

构造方法主要用于在创建类实例时，初始化类实例的内部状态

如果没有显式提供任何形式的构造方法，编译器隐式提供一个无参的默认构造方法

5. sealed 密封类
sealed 关键字用于阻止继承

被 sealed 修饰的类不能被其他类继承

被 sealed 修饰的方法不能在派生类中重写

示例：sealed class MyClass

6. object 类型
object 是 C# 中所有类型的基类

所有类、结构体、接口最终都直接或间接继承自 object，因此任何类型都拥有 object 中定义的成员

类没有显式指定父类时，默认继承 object

正因为所有类型都继承自 object，所以才能用 object 统一表示任意类型

7. 继承的使用场景
适合使用继承的情况：

类之间存在明显的 is - a (是一个) 关系，例如人是生物

多个类有大量相同的属性和方法

在原有类的基础上扩展新功能

不适合继承：

不具有 is-a 的关系

C#不允许多重继承，但不限制继承次数  
1. 访问修饰符
C#有6种访问修饰符：

访问修饰符	含义
public	可在任意位置访问
protected	可以在当前类或派生类访问
private	仅限定义该成员的类型内部访问
internal	可在当前程序集访问
protected internal	可以在当前类或派生类，或者当前程序集的任何类型访问
private protected	仅当前程序集定义该成员的类或派生类可访问
2. 方法隐藏
方法隐藏：子类定义一个与父类同名的方法，使用new关键字隐藏父类方法

方法隐藏不要求基方法virtual / abstract / override

根据引用类型决定调用哪个方法

不建议使用方法隐藏

3. 方法重写（覆盖）
重写方法指的是，子类为从基类继承的成员方法提供了新的实现，重写有如下要求：

被重写的基方法必须是 virtual / abstract / override

重写时必须使用关键字 override

方法签名、返回值、可访问性必须相同

不能重写非虚方法或static方法

属性也是类似

4. 虚方法简介
虚方法（Virtual Method）指的是允许子类重写、并在运行时根据对象的实际类型，决定调用哪个版本的方法

8.25：1. 抽象类
抽象类是一种不能被直接实例化的类，使用abstract关键字声明

抽象类既可以包含抽象方法，也可以包含普通方法和字段

子类必须实现抽象类的所有抽象方法，除非子类也是抽象类

抽象类主要用于：提供部分已实现的公共功能，并规定子类必须实现的核心行为

2. 接口
接口是一种“约定”：只定义能做什么（声明），不提供任何具体实现

接口的所有成员隐式public

接口命名时以大写字母I开头

C#禁止类多重继承，但是允许实现多个接口

3. 枚举
枚举是一种值类型，用于表示一组固定的、有限的常量集合。

枚举的底层类型默认是int，也可以显式指定为其他类型

通过枚举可以提高代码可读性，并保障类型安全

9.4：结构体简介

结构体struct是一种值类型，用于封装一组相关的数据成员

结构体也可以有字段、属性、方法、构造函数

结构体和类最本质的区别是，结构体是值类型

使用结构体之前必须初始化全部成员变量  
结构体的主要应用

结构体适合描述一系列轻量、简单的数据，例如：坐标、颜色、范围等简单数值结构

结构体是隐式密封（sealed）的，因此不可作为其他类的基类，但是可以实现接口  
重载运算符注意事项

不是所有运算符都能重载

重载后的运算符也要贴近运算符原有含义

慎用隐式自定义类型转换

9.5：装箱

把值类型转换成引用类型的过程称为装箱

装箱的本质是在堆上创建一个对象，把值类型的数据“打包”装进去，并返回一个引用

任何类型都直接或间接派生自object类，因此任何值类型都可以隐式转换为object类型

代码示例：

int a = 10;
object obj = a;  // 装箱  
装箱在堆上创建了值类型的副本，返回的是堆上副本的引用，而非原始数据的引用  
拆箱

拆箱指的是将装箱后的对象转换为值类型的过程

拆箱是显式类型转换

拆箱为非原始类型时会抛出InvalidCastException异常

is 和 as 运算符

is判断某个对象是否是指定类型或其派生类型，并返回bool

is可用于引用转换、装箱拆箱转换

代码示例：bool b = obj is int; // true

as尝试把对象转换为指定的引用类型或可空类型，成功返回该类型对象，失败返回null

as可用于引用转换和装箱转换，不能用于值类型转换（因此不可用作拆箱）

is和as都不能用于自定义类型转换  
委托

委托是可以指向方法的引用类型

通过委托，可以把方法作为参数传递或者支持事件模型等

使用 delegate 关键字声明委托

传统的方法调用是固定的，调用者和被调用者耦合度高，使用委托可以解耦调用关系，而且支持多播（一次性调用多个方法）